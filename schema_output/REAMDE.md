# schema_output

This directory stores all **generated outputs** from the project, including schemas and reports.

- `schema/` – Contains **inferred schemas** of the input datasets.  
- `reports/` – Contains **summary reports, statistics, and visualizations**.
