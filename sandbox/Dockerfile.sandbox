FROM python:3.11-slim

RUN apt-get update && apt-get install -y build-essential \
    && rm -rf /var/lib/apt/lists/*

RUN pip install pandas numpy matplotlib seaborn plotly kaleido

WORKDIR /sandbox
RUN mkdir -p /sandbox/code /sandbox/output /sandbox/data

CMD ["bash"]
